import { render, fireEvent } from '@testing-library/react';

import Input from './Input';

/**
 *
 * Input 컴포넌트 // 랜더하면 // '할 일', '추가', '할 일을 입력해 주세요' 출력
 * Input 컴포넌트 // value와 onChange 를 props로 받으면 // input의 value 와 onChange 이벤트 핸들러로 등록
 * Input 컴포넌트 // onClick 을 props로 받으면 // button의 onClick 이벤트 핸들러로 등록
 */

describe('', () => {
  context('', () => {
    it('', () => {
      console.log('');
    });
  });
});
