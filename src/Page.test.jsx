describe('<Page />', () => {
  it('renders title, Input, List', () => {});

  it('renders text when tasks does not exist ', () => {});

  it('creates new task', () => {});

  it('removes task', () => {});
});
