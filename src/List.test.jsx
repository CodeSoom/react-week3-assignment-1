describe('List Component', () => {
  context('task 가 없을 경우', () => {
    it("task가 없을 경우, '할 일이 없어요!' 출력", () => {});
  });

  context('task가 있는 경우', () => {
    it('task title 과 완료 버튼 렌더링', () => {});
    it('여러개 있는 경우, 여러 task title 과 완료 버튼 렌더링', () => {});
    it('완료 버튼을 클릭하면 해당 task 사라짐.', () => {});
  });
});
