import React from 'react';

import { render, fireEvent } from '@testing-library/react';

import App from './App';

test('x', () => {
  // state가 존재하는지 검사?
  // 3개 함수가 동작하는지 검사?
  // Page 컴포넌트가 존재하는지 검사?
});
