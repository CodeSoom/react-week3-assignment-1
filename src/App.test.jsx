import { render, fireEvent } from '@testing-library/react';

import App from './App';

/**
 *
 * App 컴포넌트 // 랜더하면 // Page 컴포넌트가 랜더됨
 * App 컴포넌트 // 랜더하면 // Page 컴포넌트의 프롭스로 함수들과 값들이 전달됨
 *
 * handleChangeTitle 함수 // html 요소? 이벤트?를 매개변수로 호출하면 // 새로운 state를 반환함
 * handleClickAddTask 함수 // 호출하면 // 새로운 state를 반환함
 * handleClickDeleteTask 함수 // number 타입 값을 매개변수로 호출하면 // 해당 숫자에 해당하는 tasks를 필터한 새로운 state 반환함
 *
 * useState 같은건 리액트의 함수니까 검증할 필요가 없겠죠?
 */

describe('', () => {
  context('', () => {
    it('', () => {
      console.log('');
    });
  });
});
